import React, { Component } from 'react';
import PlayerContainer from '../components/PlayerPage/PlayerContainer.jsx'

class PlayerPage extends Component {
    render() {
        return (
            <section id="PlayerPage">
                <PlayerContainer />
            </section>
        );
    }
}

export default PlayerPage;